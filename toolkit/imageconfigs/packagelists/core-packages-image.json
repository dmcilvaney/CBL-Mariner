{
    "packages": [
        "shim-unsigned",
        "grub2-efi-binary",
        "ca-certificates",
        "core-packages-base-image",
        "verity-readonly-root",
        "initramfs"
    ],
    "_comment": "Install 'initramfs' last to avoid unnecessary regeneration when other packages, such as 'kernel', are installed."
}
